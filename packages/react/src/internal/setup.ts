/**
 * Importing this file triggers `@zaemoru/elements`, which registers all
 * `zm-*` custom elements on the page. The React adapter components rely on
 * those custom elements being defined.
 */
import "@zaemoru/elements";
