# Accessibility Check Skill

For every interactive component, check:

1. Is the native element used where possible?
2. Is keyboard interaction supported?
3. Is focus visible?
4. Is disabled state real?
5. Is aria used correctly?
6. Is label association correct?
7. Does screen reader text make sense?
8. Does state change announce correctly where needed?

Prefer native HTML behavior over custom ARIA.