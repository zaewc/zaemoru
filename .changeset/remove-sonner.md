---
"@zaemoru/elements": major
"@zaemoru/react": major
---

Remove `zm-sonner` / `Sonner` — its responsibility overlapped with `zm-toast`. Compose multiple `zm-toast` elements (or use `zm-snackbar` for action-bearing notices) instead.
