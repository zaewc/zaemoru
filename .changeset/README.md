# Changesets

This folder is used by [Changesets](https://github.com/changesets/changesets) to manage versioning and changelogs for the workspace packages.

Run `pnpm changeset` to add a new entry, then `pnpm version-packages` to bump versions, and `pnpm release` to publish.
